pipeline {
  agent any
  environment {
    NAME = "A-SAMPLE-PIPELINE-01"
  }
  stages {
    stage ('Audit') {
      steps {
        sh '''
           go version
        '''
      }
    }
    stage ('Build') {
      steps {
        echo "BUILD ENVIORNOMENT::${env}"
      }
    }
  }
}